# Access Control software Seclib
This software implements Papouch GNOME hardware devices with IP modules and runs on their own Modbus protocol variant.
It accepts RFID or Barcode, asks an API to decide whether that code is valid and then sends a Relay switch signal to 
open the door. LCD displays and other devices from Papouch are also included.
